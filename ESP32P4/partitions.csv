# ESP32-P4 Spot-Welder Partition Table
# Flash: 16 MB (assumed for CrowPanel P4; verify via esptool.py flash_id)
#
# The full WiFi provisioning + HTTP server + captive portal + mDNS pushed the
# binary past 1 MB, so we allocate 2 MB for the factory app (plenty of headroom
# for future growth).
#
# OTA partitions are NOT included yet (OTA is stubbed on the P4 build). If OTA
# is needed later, shrink the factory partition and add ota_0/ota_1 + otadata.

# Name,   Type, SubType, Offset,  Size,     Flags
nvs,      data, nvs,     0x9000,  0x6000,
phy_init, data, phy,     0xf000,  0x1000,
factory,  app,  factory, 0x10000, 0x200000,
